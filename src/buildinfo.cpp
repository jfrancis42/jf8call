// buildinfo.cpp — regenerated on every build via the jf8call_build_timestamp target.
// Only this file is recompiled when the timestamp changes; all other sources are unaffected.
#include "build_timestamp.h"   // generated into CMAKE_BINARY_DIR by write_build_time.cmake
extern const long long g_buildTime = JF8CALL_BUILD_TIME;
